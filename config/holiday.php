<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Japanese Holidays API
    |--------------------------------------------------------------------------
    |
    | 祝日データの取得元。date.json は 日付 => 祝日名 の形式で複数年分を返す。
    |
    */

    'api' => [
        'url' => env('HOLIDAY_API_URL', 'https://holidays-jp.github.io/api/v1/date.json'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Cache
    |--------------------------------------------------------------------------
    |
    | API レスポンスのキャッシュ時間（秒）。祝日は更新頻度が低いため 1 日が目安。
    |
    */

    'cache' => [
        'ttl_seconds' => (int) env('HOLIDAY_CACHE_TTL', 86400),
    ],

];
